# Bank Chatbot UI

React frontend for the banking chatbot.

## Run

```bash
cd ui
npm install
npm start       # dev server at http://localhost:3000
```

## Build for production

```bash
npm run build
```

## Environment

`REACT_APP_API_URL` in `.env` points to the backend (default: http://localhost:8000).